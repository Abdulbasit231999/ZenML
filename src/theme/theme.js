import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#443E99",
    },
  },
  typography: { fontFamily: ["Rubik"], button: { textTransform: "none" } },
});
